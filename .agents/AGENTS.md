# Workspace Rules

- **Auto Git Commit & Push:** After making and verifying any code, configuration, or documentation changes, automatically stage (`git add`), commit (`git commit`), and push (`git push`) the changes to the remote Git repository.
